topology_metrics:
  _target_: src.callbacks.topology_metrics.TopologyMetricsCallback
  every_n_epochs: 10
  field_orders: [2, 3]
  save_figures: True
  save_artifacts: True
  log_reconstruction_pd: True
  log_latent_pd: True
  scale_quantile: 0.9
  topology_dimensions: [1, 2]
  input_metric: ambient
